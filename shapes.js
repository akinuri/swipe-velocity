class Circle {
    constructor(el) {
        this.el = el;
        this.radius = el.clientWidth;
    }
}

class Rectangle {
    constructor(width, height) {
        this.width = width;
        this.height = height;
    }
}